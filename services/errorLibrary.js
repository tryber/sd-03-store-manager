const invalidData = 'invalid_data';
const wrongFormat = 'Wrong id format';
const wrongFormatOrQuantity = 'Wrong product ID or invalid quantity';
const wrondFormatSaleId = 'Wrong sale ID format';

module.exports = {
  invalidData,
  wrongFormat,
  wrongFormatOrQuantity,
  wrondFormatSaleId,
};
